import type { GetShowResponseType } from '@/app/_hook/query/useShow';

// 帖子主体
export function ShowOption({ showData }: { showData: GetShowResponseType }) {
  return <div className="flex flex-col gap-4 ">5225</div>;
}
