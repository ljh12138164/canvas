{
  "name": "api",
  "scripts": {
    "dev": "pnpm run dev:api",
    "dev:api": "vercel dev --listen=8000",
    "deploy": "vercel"
  },
  "dependencies": {
    "hono": "^4.6.9"
  },
  "devDependencies": {
    "vercel": "^32.4.1"
  }
}
