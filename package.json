{
  "name": "canvas",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "dev:api": "turbo --filter=api dev",
    "dev:design": "turbo --filter=design dev",
    "dev:jebet": "turbo --filter=jebet dev && turbo --filter=api dev",
    "build:design": "turbo --filter=design build",
    "build:jebet": "turbo --filter=jebet build",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "turbo": "^2.0.14"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "prettier": "^3.3.3"
  }
}
