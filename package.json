{
  "name": "canvas",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "dev:api": "turbo --filter=api dev",
    "dev:note": "turbo --filter=api --filter=note --filter=ws-note dev",
    "dev:design": "turbo --filter=api --filter=design dev",
    "dev:jebet": "turbo --filter=api --filter=jebet dev",
    "dev:ws": "turbo --filter=api --filter=ws dev",
    "build:design": "turbo --filter=design build",
    "build:jebet": "turbo --filter=jebet build",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "packageManager": "pnpm@9.4.0",
  "devDependencies": {
    "turbo": "^2.0.14"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "prettier": "^3.3.3"
  }
}
